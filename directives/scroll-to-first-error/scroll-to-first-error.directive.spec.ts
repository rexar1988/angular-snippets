import { ScrollToFirstErrorDirective } from './scroll-to-first-error.directive';

describe('ToFirstErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToFirstErrorDirective();
    expect(directive).toBeTruthy();
  });
});
